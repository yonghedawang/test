/*
元组
<h2>类型化数组</h2>
<p><strong>元组（Tuple）</strong>是一个类型化数组，每个索引都有预定义的长度和类型。</p>
<p>元组很好，因为它们允许数组中的每个元素都是已知类型的值。</p>
<p>要定义元组，请指定数组中每个元素的类型：</p>
*/
let x:[number,string,boolean];
x = [1,'1',false];
console.log(x);

